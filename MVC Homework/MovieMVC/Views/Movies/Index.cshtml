@model IEnumerable<ApplicationCore.Model.ResponseModel.MovieResponse>

<h1>Movies</h1>

<div class="row">
    @foreach (var movie in Model)
    {
        <div class="col-md-4">
            @Html.Partial("_MovieCard", movie)
        </div>
    }
</div>
